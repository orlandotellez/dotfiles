return {
  "nvim-neo-tree/neo-tree.nvim",
  opts = {
    filesystem = {
      filtered_items = {
        visible = true, -- habilitar archivos ocultos por defecto
        hide_dotfiles = false,
        show_hidden_count = true,
      },
    },
  },
}
